import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
  return (
    <div>
      <h1>ServiceScape - 3D Organization Visualizer</h1>
      <p>Frontend workspace ready. Implementation will be added in Phase 2+.</p>
    </div>
  );
}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
